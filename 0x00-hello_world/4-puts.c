#include <stdio.h>

/**
 * main - one line print out using puts function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char test_str[] = "\"Programming is like building a multilingual puzzle";

	puts(test_str);
	return (0);
}
